// use crate::syntax::ast;
// use std::collections::HashMap;

// fn check_module(module: &ast::Module) {
//     // let mut context = HashMap::new();

//     for declaration in &module.declarations {}
// }

// enum Prototype {}

// enum DeclarationPrototype {}
